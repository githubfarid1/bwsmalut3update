1.tambah report berkas yang masih dipinjam
2. ada data items yang belum di syncronize (14 item):
SELECT a.item_number, b.bundle_number, c.box_number, a.token FROM arsip_tata_item a, arsip_tata_bundle b, arsip_tata_box c WHERE a.bundle_id=b.id AND b.box_id=c.id AND  a.yeardate=2024 AND a.created_by_id IS NOT null
atau:
SELECT a.item_number, b.bundle_number, c.box_number, a.token FROM arsip_tata_item a, arsip_tata_bundle b, arsip_tata_box c WHERE a.bundle_id=b.id AND b.box_id=c.id AND  a.yeardate=2024 AND a.token IS NULL 

3. ada data items yang sudah di scan yang filenya tidak bisa diambil data infotime nya (15 item). caranya dengan mendownload lalu filter "waktu pelaksanaan" yang kosong.


4. tambhkan doc_type (buku, gambar, dll) pada table item.


Proses Searching:
1. Pertama kita login ke aplikasi dengan alamat:
https://arsip.bwsmalut.my.id
pada tampilan awal kita akan melihat daftar dokumen yang sudah diinput ke aplikasi earsip kami. dokumen yang sudah ada di scanning dalam format PDF ditandai dengan adanya gambar sample depan dokumen, sedangkan yang belum discan tidak ada gambarnya.
untuk melihat cover depan kita cukup mengklik gambar tersebut.
untuk melakukan download kita cukup menekan tombol download.
2. 